<#assign className = table.className>
<#assign classNameFirstLower = table.classNameFirstLower>   
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<#macro mapperEl value>${r"#{"}${value}}</#macro>
<#macro namespace>${className}.</#macro>
<mapper namespace="${basepackage}.mybatis.${className}MybatisDao">

	<resultMap id="RM_${className}" type="${basepackage}.entity.${className}">
        <#list table.columns as column>
        <result property="${column.columnNameLower}" column="${column.sqlName}"/>
		</#list>
	</resultMap>
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="${className}_columns">
		<#list table.columns as column>${column.sqlName}<#if column_has_next>,</#if></#list>
	</sql>
	
	<!-- 添加 -->
	<insert id="save" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ${table.sqlName} (
        <#list table.columns as column>
        	${column.sqlName} <#if column_has_next>,</#if>
        </#list>
        ) VALUES (	
        <#list table.columns as column>
        	<@mapperEl column.columnNameFirstLower/> <#if column_has_next>,</#if>
        </#list>        
        )
	</insert>
    
	<!-- 修改 -->
	<update id="update" >
        UPDATE ${table.sqlName} 
		<set>
	        <#list table.notPkColumns as column>	        
			<if test="@com.zjgj.crm.util.Ognl@isNotEmpty(${column.columnNameFirstLower})">${column.sqlName} = <@mapperEl column.columnNameFirstLower/> <#if column_has_next>,</#if></if>
	        </#list>
		</set>
        WHERE 
        	<#list table.compositeIdColumns as column>
	        ${column.sqlName} = <@mapperEl column.columnNameLower/> <#if column_has_next>,</#if>
	        </#list>	        
	</update>

	<!-- 删除 -->
    <delete id="delete">
		DELETE FROM ${table.sqlName} WHERE
        <#list table.compositeIdColumns as column>
        ${column.sqlName} = <@mapperEl 'id'/> <#if column_has_next>,</#if>
		</#list>
    </delete>
	
	<!-- 批量删除 -->
	<delete id="batchDelete" parameterType="java.util.List">
        DELETE FROM  ${table.sqlName}  WHERE
        <#list table.compositeIdColumns as column>${column.sqlName}<#if column_has_next></#if></#list> IN 
        <foreach item="item" collection="idsList" open="(" separator="," close=")">
			<@mapperEl 'item'/>
		</foreach>  
    </delete>
    
	<!-- 根据主键获取对象 -->
    <select id="getByID" resultMap="RM_${className}">
		SELECT <include refid="${className}_columns" />
		FROM ${table.sqlName} WHERE 
		<#list table.compositeIdColumns as column>
		${column.sqlName} = <@mapperEl 'id'/> <#if column_has_next>,</#if>
		</#list>    
	</select>
	
	<!-- 获取总条数 -->
	<select id="countAll" resultType="int">
        SELECT count(<#list table.compositeIdColumns as column>${column.sqlName}</#list>) FROM ${table.sqlName} 
    </select>
	
	<!-- 获取所有 -->
	<select id="findAll" resultMap="RM_${className}">
        SELECT <include refid="${className}_columns" /> 
		FROM ${table.sqlName} 
    </select>
	
	<!-- 提取公共WHERE -->
	<sql id="${className}_where">
		<where>
			 
	       <#list table.columns as column>
	       <#if column.isDateTimeColumn>
	       <if test="@com.zjgj.crm.util.Ognl@isNotEmpty(${column.columnNameFirstLower}Begin)">
				AND ${column.sqlName} >= <@mapperEl column.columnNameFirstLower+"Begin"/>
		   </if>
		   <if test="@com.zjgj.crm.util.Ognl@isNotEmpty(${column.columnNameFirstLower}End)">
				AND ${column.sqlName} &lt;= <@mapperEl column.columnNameFirstLower+"End"/>
		   </if>
	       <#else>
	       <if test="@com.zjgj.crm.util.Ognl@isNotEmpty(${column.columnNameFirstLower})">
				AND ${column.sqlName} = <@mapperEl column.columnNameFirstLower/>
			</if>
	       </#if>
	       </#list>			
		</where>
	</sql>
	
	<!-- 根据条件获取条数 -->
    <select id="findNumberByCondition" resultType="int">
        SELECT count(<#list table.compositeIdColumns as column>${column.sqlName}</#list>) FROM ${table.sqlName} 
		<include refid="${className}_where"/>    
    </select>
    
	<!-- 根据条件获取集合 -->
    <select id="findPageBreakByCondition" resultMap="RM_${className}">
    	SELECT <include refid="${className}_columns" />
	    FROM ${table.sqlName} 
		<include refid="${className}_where"/>
		
		<if test="@com.zjgj.crm.util.Ognl@isNotEmpty(sortColumns)">
			ORDER BY <@jspEl 'sortColumns'/>
		</if>
    </select>
</mapper>

